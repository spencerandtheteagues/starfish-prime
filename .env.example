# ElderCare Environment Variables
# Copy this file to .env and fill in your actual values

# ⚠️ IMPORTANT: Anthropic API key is now in functions/.env (server-side only)
# Never put API keys in the client app!

# Firebase Configuration (client-side)
# NOTE: The actual Firebase config is in GoogleService-Info.plist (iOS)
# and google-services.json (Android). These are loaded automatically by
# React Native Firebase. No need to set environment variables for Firebase.

# Optional: Sentry for error tracking
# SENTRY_DSN=your-sentry-dsn

# Optional: RevenueCat for subscriptions
# REVENUECAT_API_KEY=your-revenuecat-key

# MCP Server URL (for OpenAI Realtime Voice Integration)
EXPO_PUBLIC_MCP_SERVER_URL=http://localhost:3000
