import './globals.css'; export const metadata={title:'Neon Builder — Final'}; export default function RootLayout({children}:{children:React.ReactNode}){return(<html lang='en'><body><div style={{maxWidth:1200,margin:'0 auto',padding:24}}><h1 style={{fontSize:28,fontWeight:800,letterSpacing:1,color:'var(--neon)'}}>⚡ Neon Builder — Final</h1><p style={{color:'var(--muted)'}}>Prompt → plan → scaffold → CRUD → tests → auto-fix → PR.</p>{children}</div></body></html>)}
