# DATA SECURITY STATEMENT — SilverGuard
Date: 2026-01-11

- No AI provider keys in mobile apps.
- Keys stored server-side (Firebase Functions config/Secret Manager).
- Encrypt in transit (TLS).
- Restrict staff access (least privilege).
- Prefer summary logs; avoid raw transcripts.
- No ambient audio storage.
